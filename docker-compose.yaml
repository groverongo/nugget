services:
  postgres:
    image: postgres:16-alpine
    container_name: nugget-postgres
    environment:
      POSTGRES_USER: nugget
      POSTGRES_PASSWORD: nugget
      POSTGRES_DB: nugget
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: